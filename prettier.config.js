export default {
  singleQuote: true, // Use single quotes instead of double quotes
  semi: false, // Omit semicolons at the end of statements
  trailingComma: 'es5', // Add trailing commas where valid in ES5 (objects, arrays, etc.)
  bracketSpacing: true, // Add spaces between brackets in object literals
  jsxSingleQuote: false, // Use double quotes in JSX (to differentiate from JavaScript strings)
  arrowParens: 'avoid', // Omit parentheses when there's only one arrow function parameter
  printWidth: 80, // Set the maximum line length to 80 characters
  tabWidth: 2, // Set indentation to 2 spaces
  useTabs: false, // Use spaces instead of tabs for indentation
  endOfLine: 'lf', // Use 'lf' (line feed) for end-of-line (prevents issues with different OS)
}
